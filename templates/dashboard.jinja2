{% extends "base.html" %}
{% block title %}Dashboard - Expense Splitter{% endblock %}

{% block content %}
<div class="grid" style="margin-bottom: 30px;">
    <div class="stat-card">
        <h3>{{ groups | length }}</h3>
        <p>Nhóm tham gia</p>
    </div>
    <div class="stat-card" style="background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);">
        <h3>{{ groups | default([]) | map(attribute='expense_count') | sum }}</h3>
        <p>Tổng lượt chi tiêu</p>
    </div>
</div>

<div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px;">
    <h2>Nhóm của bạn</h2>
    <button onclick="showCreateGroup()" class="btn btn-primary">➕ Tạo nhóm mới</button>
</div>

<div id="createGroupModal" class="modal" style="display:none;">
    <div class="modal-content" style="max-width: 500px;">
        <div class="modal-header">
            <span class="close" onclick="closeCreateGroup()">&times;</span>
            <h2>Tạo nhóm mới</h2>
        </div>
        <form method="POST" action="{{ url_for('create_group') }}">
            <div class="form-group">
                <label>Tên nhóm</label>
                <input type="text" name="name" class="form-control" placeholder="VD: Du lịch Đà Lạt, Ăn trưa..." required>
            </div>
            <div class="form-group">
                <label>Tiền tệ</label>
                <select name="currency" class="form-control">
                    <option value="VND">VND (đ)</option>
                    <option value="USD">USD ($)</option>
                    <option value="EUR">EUR (€)</option>
                </select>
            </div>
            <div class="form-group">
                <label>Thành viên</label>
                <div id="members-container">
                    <div class="member-input-row" style="display: flex; gap: 10px; margin-bottom: 10px;">
                        <input type="text" name="members[]" class="form-control" placeholder="Tên thành viên 1">
                        <button type="button" class="btn" style="background: #ff4757; color: white; padding: 0 10px;" onclick="removeMember(this)">✕</button>
                    </div>
                </div>
                <button type="button" class="btn" style="background: #f0f0f0; color: #333; width: 100%; border: 1px dashed #ccc; margin-top: 5px;" onclick="addMemberRow()">
                    ➕ Thêm thành viên khác
                </button>
            </div>
            <div style="margin-top: 20px; text-align: right;">
                <button type="submit" class="btn btn-success" style="width: 100%;">🚀 Tạo nhóm ngay</button>
            </div>
        </form>
    </div>
</div>

{% if groups %}
<div class="grid">
    {% for group in groups %}
    <div class="card" onclick="window.location='{{ url_for('group_detail', group_id=group['id']) }}'" style="cursor: pointer; 
        {% if group['name'] == 'Chi tiêu cá nhân' %} border: 2px solid #667eea; {% endif %}">
        
        <h3 style="color: #667eea; margin-bottom: 10px;">
            {{ group['name'] }} 
            {% if group['name'] == 'Chi tiêu cá nhân' %} <span style="font-size:0.6em; background:#667eea; color:white; padding:2px 5px; border-radius:4px;">Cá nhân</span> {% endif %}
        </h3>
        <p style="color: #666; margin-bottom: 10px;">
            👥 {{ group['member_count'] }} thành viên
        </p>
        <p style="color: #666; margin-bottom: 10px;">
            🧾 {{ group['expense_count'] }} chi tiêu
        </p>
        <p style="font-weight: bold; color: #11998e; font-size: 1.2em;">
            {{ "{:,.0f}".format(group['total_amount'] or 0) }} {{ group['currency'] }}
        </p>
    </div>
    {% endfor %}
</div>
{% else %}
<div style="text-align: center; padding: 50px; color: #999;">
    <p style="font-size: 1.2em;">Chưa có nhóm nào. Hãy tạo nhóm đầu tiên!</p>
</div>
{% endif %} 

<script>
function showCreateGroup() { document.getElementById("createGroupModal").style.display = "block"; }
function closeCreateGroup() { document.getElementById("createGroupModal").style.display = "none"; }
function addMemberRow() {
    const container = document.getElementById('members-container');
    const div = document.createElement('div');
    div.className = 'member-input-row';
    div.style.cssText = 'display: flex; gap: 10px; margin-bottom: 10px;';
    div.innerHTML = `<input type="text" name="members[]" class="form-control" placeholder="Tên thành viên tiếp theo..." required><button type="button" class="btn" style="background: #ff4757; color: white; padding: 0 10px;" onclick="removeMember(this)">✕</button>`;
    container.appendChild(div);
    div.querySelector('input').focus();
}
function removeMember(btn) {
    const container = document.getElementById('members-container');
    if (container.children.length > 0) btn.parentElement.remove();
}
window.onclick = function(event) {
    if (event.target == document.getElementById("createGroupModal")) document.getElementById("createGroupModal").style.display = "none";
}
</script>
{% endblock %}