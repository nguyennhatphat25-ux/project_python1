{% extends "base.html" %}
{% block title %}Dashboard - Expense Splitter{% endblock %}

{% block content %}
<div class="grid">
    <div class="stat-card">
        <h3>{{ groups | length }}</h3>
        <p>Nhóm</p>
    </div>
    <div class="stat-card" style="background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);">
        <h3>{{ groups | default([]) | map(attribute='expense_count') | sum }}</h3>
        <p>Chi tiêu</p>
    </div>
</div>

<div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px;">
    <h2>Nhóm của bạn</h2>
    <button onclick="showCreateGroup()" class="btn btn-primary">➕ Tạo nhóm mới</button>
</div>

<!-- Modal tạo nhóm -->
<div id="createGroupModal" class="modal" style="display:none;">
    <div class="modal-content">
        <div class="modal-header">
            <span class="close" onclick="closeCreateGroup()">&times;</span>
            <h2>Tạo nhóm mới</h2>
        </div>
        <form method="POST" action="{{ url_for('create_group') }}">
            <div class="form-group">
                <label>Tên nhóm</label>
                <input type="text" name="name" class="form-control" placeholder="Nhập tên nhóm" required>
            </div>

            <div class="form-group">
                <label>Tiền tệ</label>
                <select name="currency" class="form-control">
                    <option value="VND">VND</option>
                    <option value="USD">USD</option>
                    <option value="EUR">EUR</option>
                </select>
            </div>

            <div class="form-group">
                <label>Thêm thành viên (phân tách bằng dấu ,)</label>
                <input type="text" name="members" class="form-control" placeholder="Tên thành viên 1, Tên thành viên 2">
            </div>

            <button type="submit" class="btn btn-success" style="width: 100%;">Tạo nhóm</button>
        </form>
    </div>
</div>

{% if groups %}
<div class="grid">
    {% for group in groups %}
    <div class="card" onclick="window.location='{{ url_for('group_detail', group_id=group['id']) }}'" style="cursor: pointer;">
        <h3 style="color: #667eea; margin-bottom: 10px;">{{ group['name'] }}</h3>
        <p style="color: #666; margin-bottom: 10px;">
            👥 {{ group['member_count'] }} thành viên
        </p>
        <p style="color: #666; margin-bottom: 10px;">
            🧾 {{ group['expense_count'] }} chi tiêu
        </p>
        <p style="font-weight: bold; color: #11998e; font-size: 1.2em;">
            {{ "{:,.0f}".format(group['total_amount'] or 0) }} {{ group['currency'] }}
        </p>
    </div>
    {% endfor %}
</div>
{% else %}
<div style="text-align: center; padding: 50px; color: #999;">
    <p style="font-size: 1.2em;">Chưa có nhóm nào. Hãy tạo nhóm đầu tiên!</p>
</div>
{% endif %} 

<!-- JS Modal -->
<script>
function showCreateGroup() {
    document.getElementById("createGroupModal").style.display = "block";
}
function closeCreateGroup() {
    document.getElementById("createGroupModal").style.display = "none";
}
window.onclick = function(event) {
    var modal = document.getElementById("createGroupModal");
    if (event.target == modal) {
        modal.style.display = "none";
    }
}
</script>
{% endblock %}
